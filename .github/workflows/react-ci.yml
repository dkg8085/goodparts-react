name: React CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      # If you have tests and want coverage, uncomment this step
      # - name: Run Tests with Coverage
      #   run: npm test -- --coverage --watchAll=false

      - name: Build Project
        run: npm run build

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          # Pass SonarCloud properties via 'args'
          args: >
            -Dsonar.projectKey=dkg8085_goodparts-react
            -Dsonar.organization=dkg8085
            -Dsonar.sources=src
            -Dsonar.exclusions=**/node_modules/**
            # If you have coverage reports, uncomment the next line
            # -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info